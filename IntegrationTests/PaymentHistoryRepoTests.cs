using System;

namespace IntegrationTests
{
    public class PaymentHistoryRepoTests
    {
        // These these tests would live wherever the PaymentHistoryRepo is put in a real world scenario. 
        // Aka this would be a seperate package to be reused throughout the project, taking in a connection string
        // to know where to look in each env.
        
        // This is just ot ensure the database layer of the project is working as expected. The model of the PaymentRequest
        // would also live in the other project and we would map to it. These aren't needed in this case as there is no actual
        // database. 
    }
}